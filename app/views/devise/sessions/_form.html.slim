= form_for resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-signin' }, remote: true do |f|
    h2.form-signin-heading Please login
    = f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'Email', data: { disable: true }
    = f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Password', data: { disable: true }
    - if devise_mapping.rememberable?
      = f.check_box :remember_me
      |  
      = f.label :remember_me
    .actions
      = f.submit "Log in", class: 'btn btn-lg btn-primary btn-block', data: { disable: true }
    br
    = render "devise/shared/links"